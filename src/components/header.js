import * as React from "react"
import { Link } from "gatsby"

const Header = ({ siteTitle }) => (
  <header
    className=""
  >
   <h1 className="">Nguyễn Hoàng Kha</h1>
  </header>
)

export default Header
